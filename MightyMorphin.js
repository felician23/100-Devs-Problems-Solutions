let myfav = "Halloween";
myfav = myfav.toUpperCase();
console.log(myfav);

let myArr = "welcome"
alert(myArr.slice(-3))

function takesInFive(a, b, c, d, e) {
    return Math.abs(a - b - c - d - e - 100)
}

takesInFive()

function takesInThree(a, b, c) {
    console.log(Math.min(a,b,c))
    console.log(Math.max(a,b,c))
}

function headsTails(a) {
    
}